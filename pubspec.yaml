name: sudo_jwt
description: A new Flutter package project.
version: 1.0.0
homepage: https://sudo.dog

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
